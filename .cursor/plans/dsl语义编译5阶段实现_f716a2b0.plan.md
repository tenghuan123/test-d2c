---
name: DSL语义编译5阶段实现
overview: 按 tree-dfs-and-bfs.md 规范，重写 semantic-inference.cjs 实现 5 阶段受控树搜索
todos:
  - id: stage1
    content: 实现 Stage1 Shallow BFS (maxDepth=2)
    status: completed
  - id: stage2
    content: 实现 Stage2 Skeleton Freeze 机制
    status: completed
  - id: stage3
    content: 实现 Stage3 Targeted DFS (maxDepth=5)
    status: completed
  - id: stage4
    content: 实现 Stage4 Repetition Detection
    status: completed
  - id: test
    content: 测试完整流程
    status: completed
isProject: false
---

## 目标

按文档规范实现完整的 5 阶段语义编译算法。

## 当前问题

- 没有区分 BFS/DFS 阶段
- AI 推断范围不精准
- 缺少结构冻结机制

## 实现计划

### Stage 1: Shallow BFS (宏观扫描)

```js
function stage1ShallowBFS(tree, maxDepth = 2) {
  // 只展开到深度 2
  // 识别页面类型：Header/Section/Footer/List
  // 收集所有 Level 0-2 节点
}
```

### Stage 2: Skeleton Freeze (结构冻结)

```js
function stage2Freeze(skeleton) {
  // 锁定层级、顺序、角色
  // 标记 frozen = true
}
```

### Stage 3: Targeted DFS (局部深度搜索)

```js
function stage3TargetedDFS(frozenSkeleton, maxDepth = 5) {
  // 只对 Content/List/Section 节点执行 DFS
  // AI 推断子节点角色
  // 禁止修改父节点
}
```

### Stage 4: Repetition Detection (组件抽象)

```js
function stage4DetectRepetition(skeleton) {
  // 检测结构相似度 > 85%
  // 重复次数 ≥ 3
  // 生成 component-map.json
}
```

### Stage 5: Code Generation (可选)

- 生成 React/Vue 组件代码

## 改动文件

- `scripts/semantic-inference.cjs` - 完全重写

## 核心约束

- BFS depth ≤ 2（宏观）
- DFS depth ≤ 5（微观）
- 冻结后不可修改
- AI 仅做评分/分类，不做决策

